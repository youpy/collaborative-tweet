development:
  clients:
    default:
      database: mongoid_dev
      hosts:
        - localhost:27017
production:
  clients:
    default:
      uri: <%= ENV['MONGODB_URI'] %>
      options:
        retry_writes: false
